#include <stdio.h>

int main()
{ 
    unsigned   int x = 1;
    int  a =1 ;
    while (x>0){
    x<<1;++a;
    }
    int n = a-1; 
    int max_int=(1<<n);   
    printf("The max value of int on a machine:%d\n",max_int);
    printf bit [ a]   ;
    
}